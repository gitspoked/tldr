# Local-first defaults
OLLAMA_URL=http://localhost:11434
QDRANT_URL=http://localhost:6333
# Optional for API-key-protected Qdrant. Read from the environment only.
QDRANT_API_KEY=
FALKORDB_URL=redis://localhost:6379
TLDREADME_EMBED_MODEL=ollama/mxbai-embed-large
TLDREADME_CHAT_MODEL=ollama/qwen2.5-coder:3b-instruct
TLDREADME_MODEL_TIMEOUT_SECONDS=15
# Lower this to 8 or 16 when memory is constrained. The hard maximum is 128.
TLDREADME_EMBED_BATCH_SIZE=32

# Optional LiteLLM proxy
# Leave blank for direct local Ollama. Set this when using docker-compose.llm.yml
# or another LiteLLM endpoint.
LITELLM_URL=

# Optional cloud provider credentials for LiteLLM
# TLDREADME reads LITELLM_API_KEY or LITELLM_MASTER_KEY only from the
# environment when the proxy itself requires authentication.
LITELLM_API_KEY=
LITELLM_MASTER_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
